{
  "devDependencies": {
    "@commitlint/cli": "^19.8.0",
    "@commitlint/config-conventional": "^19.8.0",
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky",
    "pre-commit": "go fmt ./... && go vet ./... && go test ./..."
  }
}
